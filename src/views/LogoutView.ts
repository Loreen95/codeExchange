// This has no reason to exist, The userstory demanded we make the logout button accessable everywhere.
// To achieve this I placed this inside of the global script.ts because otherwise I had to include this file. EVERYWHERE

// const Logout: LogoutClass = new LogoutClass();
// import { LogoutClass } from "../controllers/LogoutController";
// const logoutBtn: HTMLButtonElement = document.querySelector("#logoutButton")!;

// logoutBtn.addEventListener("click", async () => {
//     await Logout.logoutFunction();
// });
